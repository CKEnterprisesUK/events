# Versioned raw SQL (phpMyAdmin deployment)

Production runs on shared cPanel hosting with **no SSH access** — only
phpMyAdmin. Laravel migrations remain the single source of truth and run
locally/CI, but every schema change also produces a matching, generated raw
`.sql` file here so the schema can be applied on prod purely by pasting SQL.

## How to apply on prod

Paste the files below into phpMyAdmin (Import / SQL tab) **in filename order**.
Each file's header documents exactly which migration(s) it was generated from
and the command used to regenerate it.

## Rules

- **Generated, never hand-written.** DDL is produced from the migrated MySQL
  schema (`mysqldump` / `php artisan schema:dump`). Regenerate the affected file
  whenever its migration changes so the SQL stays byte-consistent with the
  migrations.
- Files are versioned with an incrementing numeric prefix (`NNN_...`).
- Each file records its migration(s) in the Laravel `migrations` ledger so the
  app's migration state stays consistent if migrations are later run against the
  database.

## Files

| File | Applied? | Description | Generated from |
| --- | --- | --- | --- |
| `001_create_queue_tables.sql` | ☐ | `migrations`, `jobs`, `job_batches`, `failed_jobs` tables (DB queue foundation). | `0001_01_01_000002_create_jobs_table` + framework `migrations` table |
| `002_create_companies.sql` | ☐ | `companies` table (tenant root: slug, status, fee handling, Stripe, branding). | `2024_01_01_000100_create_companies_table` |
| `003_create_platform_settings.sql` | ☐ | `platform_settings` table (Platform-wide config: `global_fee_percent`). | `2024_01_01_000900_create_platform_settings_table` |
| `004_seed_platform_settings.sql` | ☐ | Seed the single default `platform_settings` row (`global_fee_percent` = 5.00). | `Database\Seeders\PlatformSettingSeeder` |
| `005_create_users.sql` | ☐ | `users` table (Company_Users + Super_Admins: four-role enum, `company_id` FK NULL, `last_activity_at`, single-Owner invariant via generated-column UNIQUE index). Apply after `002_create_companies.sql`. | `0001_01_01_000000_create_users_table` + FK from `2024_01_01_000100_create_companies_table` |
| `006_create_events.sql` | ☐ | `events` table (Company-owned events: `company_id` FK, details, `capacity` NULL = unlimited, `is_published`, per-Event branding overrides). Apply after `002_create_companies.sql`. | `2024_01_01_001000_create_events_table` |
| `007_create_invitations.sql` | ☐ | `invitations` table (Company-owned invitations: `company_id` FK, `email`, `role` enum excluding owner, unique `token`, `accepted_at` NULL while pending, `expires_at`). Apply after `002_create_companies.sql`. | `2024_01_01_001100_create_invitations_table` |
| `008_create_ticket_types.sql` | ☐ | `ticket_types` table (Company-owned ticket categories: `company_id` + `event_id` FKs, `name` VARCHAR(100), `price_minor` integer minor units (0 = free), `capacity`, `sold_count`/`reserved_count` default 0, `sale_starts_at`/`sale_ends_at` window). Apply after `002_create_companies.sql` and `006_create_events.sql`. | `2024_01_01_001200_create_ticket_types_table` |
| `009_create_orders.sql` | ☐ | `orders` table (Company-owned Customer checkouts: `company_id` + `event_id` FKs, `order_reference` UNIQUE across Platform, `status` enum default `reserved`, money fields in integer minor units default 0, `fee_handling_mode` snapshot, `reserved_until` 900s window, nullable Stripe linkage + scan columns with `scanned_by` FK to `users` ON DELETE SET NULL). Apply after `002_create_companies.sql`, `005_create_users.sql`, and `006_create_events.sql`. | `2024_01_01_001300_create_orders_table` |
| `010_create_tickets.sql` | ☐ | `tickets` table (Company-owned per-ticket rows: `company_id` + `order_id` + `ticket_type_id` FKs cascading on delete, `status` enum(`valid`,`voided`) default `valid`). Apply after `008_create_ticket_types.sql` and `009_create_orders.sql`. | `2024_01_01_001400_create_tickets_table` |
| `011_create_order_consents.sql` | ☐ | `order_consents` table (Company-owned consent capture: `company_id` + `order_id` FKs cascading on delete, `consent_key`, `accepted` boolean, `captured_at` timestamp). Apply after `009_create_orders.sql`. | `2024_01_01_001500_create_order_consents_table` |
| `012_create_processed_webhooks.sql` | ☐ | `processed_webhooks` table (webhook idempotency ledger: `stripe_event_id` UNIQUE, `type`, `processed_at` timestamp; not Company-owned, no foreign keys). Apply after `001_create_queue_tables.sql`. | `2024_01_01_001600_create_processed_webhooks_table` |
| `013_add_fulfilled_at_to_orders.sql` | ☐ | ALTER `orders` to add nullable `fulfilled_at` timestamp (fulfilment idempotency guard: capacity committed reserved→sold, QR issued, ticket email enqueued exactly once on confirmation). Apply after `009_create_orders.sql`. | `2024_01_01_001700_add_fulfilled_at_to_orders_table` |
| `014_create_sessions.sql` | ☐ | `sessions` table (database session driver store: `id` PK, nullable `user_id`, `ip_address`, `user_agent`, `payload`, `last_activity`). Required because `SESSION_DRIVER=database`; every `web` route reads/writes it via StartSession. Apply after `005_create_users.sql`. | `0001_01_01_000000_create_users_table` (framework `sessions` table) |
| `015_create_cache_tables.sql` | ☐ | `cache` + `cache_locks` tables (database cache store: `key` PK, `value`, `expiration`; locks add `owner`). Required because `CACHE_STORE=database`; the `ResolveTenant` middleware memoises Company-by-slug lookups via `cache()->remember()` on every storefront request. | `0001_01_01_000001_create_cache_table` |
| `016_add_contacts_to_companies.sql` | ☐ | ALTER `companies` to add nullable `support_email` (public support contact) and `gdpr_contact_email` (GDPR/data-protection contact) columns, managed by the Owner and part of the dashboard onboarding checklist. Apply after `002_create_companies.sql`. | `2024_01_01_000101_add_contacts_to_companies_table` |
| `017_add_legal_details_to_companies.sql` | ☐ | ALTER `companies` to widen `status` enum (add `pending`/`closed`, default stays `active`) and add legal/registration + address columns (`legal_name`, `trading_name`, `organisation_type` enum, `company_number`, `charity_number`, `website`, `email`, `phone`, `address_line_1`, `address_line_2`, `city`, `postcode`, `country` char(2) default `GB`) captured at signup and managed on the branding/settings surface. Apply after `002_create_companies.sql` and `016_add_contacts_to_companies.sql`. | `2024_01_01_000102_add_legal_details_to_companies_table` |
| `018_add_poster_to_companies.sql` | ☐ | ALTER `companies` to add a nullable `poster_path` column (optional Storefront hero/poster image shown at the top of the public Storefront, alongside the Company `logo_path`). Apply after `002_create_companies.sql`. Ledger row for the shared migration is recorded in `019_add_poster_to_events.sql`. | `2024_01_01_001800_add_poster_to_branding` |
| `019_add_poster_to_events.sql` | ☐ | ALTER `events` to add a nullable `poster_path` column (optional per-Event poster/hero image shown on the Event page and as a Storefront thumbnail, overriding the Company hero; alongside the per-Event `logo_path`). Apply after `006_create_events.sql` and `018_add_poster_to_companies.sql`. Records the shared migration in the `migrations` ledger once. | `2024_01_01_001800_add_poster_to_branding` |
| `023_add_storefront_profile_to_companies.sql` | ☐ | ALTER `companies` to add nullable public Storefront profile columns: `about_text` (an "about the company" description) plus external links `facebook_url`, `instagram_url`, `x_url`, `linkedin_url`, `terms_url` (organiser's own Terms & Conditions page) and `privacy_url` (organiser's own Privacy Notice page). The organisation `website` column already exists (017) and is reused. Apply after `002_create_companies.sql`. | `2024_01_01_002200_add_storefront_profile_to_companies_table` |
| `024_add_box_office_role.sql` | ☐ | ALTER `users` and `invitations` to add the `box_office` Company role to the `role` enum (a cut-down Admin that runs events/ticketing/orders but cannot change company settings, users, Stripe, billing, or GDPR). `users.role` keeps `owner`; `invitations.role` stays owner-excluded. Apply after `005_create_users.sql` and `007_create_invitations.sql`. | `2024_01_01_002300_add_box_office_role` |
| `025_replace_legal_urls_with_privacy_text_on_companies.sql` | ☐ | ALTER `companies` to move legal content in-app: add nullable `privacy_text` (in-app Privacy Notice shown to the customer on request at checkout, mirroring the existing `terms_text`) and drop the external-link columns `terms_url` and `privacy_url`. Apply after `023_add_storefront_profile_to_companies.sql`. | `2024_01_01_002400_replace_legal_urls_with_privacy_text_on_companies` |
| `026_add_ticket_design_to_events.sql` | ☐ | ALTER `events` to add per-Event printed-ticket design columns: nullable `ticket_instructions` (free-text entry instructions printed on the A4 ticket PDF), `sponsor_top_path` (top landscape sponsor banner) and `sponsor_bottom_path` (bottom landscape sponsor banner, also shown on the public storefront/event page). Apply after `019_add_poster_to_events.sql`. | `2024_01_01_002500_add_ticket_design_to_events` |
| `027_create_legal_documents.sql` | ☐ | CREATE `legal_documents` table (Platform-level Trust & Legal Centre: `slug` UNIQUE, `title`, `body` longtext nullable, `is_published` default 0, `sort_order` default 0). Holds Platform-wide policies (Terms & Conditions, Privacy Notice, PCI DSS statement, cookie policy, etc.) authored by a Super_Admin, published at public `/trust` URLs and linked from the footer — distinct from the per-Company `terms_text`/`privacy_text`. No foreign keys. | `2024_01_01_002600_create_legal_documents_table` |
| `028_add_agreed_to_terms_at_to_users.sql` | ☐ | ALTER `users` to add nullable `agreed_to_terms_at` timestamp recording acceptance of the Platform Terms & Conditions of Events by CK Enterprises UK, captured at self-signup. NULL for users who never explicitly accepted. Apply after `005_create_users.sql`. | `2024_01_01_002700_add_agreed_to_terms_at_to_users_table` |
| `029_create_audit_logs.sql` | ☐ | CREATE `audit_logs` table (append-only trail of security/money/access/privacy actions: nullable `company_id`/`actor_user_id`/`impersonator_user_id` FKs to companies/users ON DELETE SET NULL, `actor_type` enum, `is_impersonated` flag, `action` machine key, polymorphic `auditable_type`/`auditable_id`, `summary`, JSON `context`, `ip_address`, immutable `created_at` only). NOT tenant-scoped by the global scope — callers scope `company_id` explicitly. Apply after `002_create_companies.sql` and `005_create_users.sql`. | `2024_01_01_002800_create_audit_logs_table` |
| `030_create_reserved_slugs.sql` | ☐ | CREATE `reserved_slugs` table (Company_Slug blocklist: `slug` UNIQUE, `reason` nullable, `is_system` default 0). Slugs that may never be claimed by a Company at self-signup or slug change because they collide with reserved platform routes (`login`, `admin`, `dashboard`, `trust`, `webhooks`, ...) or infrastructure paths (`api`, `assets`, `.well-known`, ...), or are brand/abuse words the Platform declines to hand out. `is_system` rows are seeded and undeletable in the admin UI; operator-added rows are removable. Enforced by `App\Rules\CompanySlug`, managed by a Super_Admin on `/admin`. Seeds the system reserved slugs in the same file. No foreign keys. | `2024_01_01_002900_create_reserved_slugs_table` + `Database\Seeders\ReservedSlugSeeder` |
| `031_add_refunded_total_to_orders.sql` | ☐ | ALTER `orders` to add `refunded_total_minor` INT NOT NULL DEFAULT 0 (cumulative amount refunded to the Customer, integer minor units). Supports PARTIAL refunds: a paid Order can be refunded one or more times up to `order_total_minor` while staying `paid` with valid Tickets; only when cumulative refunds reach the full total does it flip to the terminal `refunded` state. Apply after `009_create_orders.sql`. | `2024_01_01_003000_add_refunded_total_to_orders_table` |
| `032_add_sponsor_details_to_events.sql` | ☐ | ALTER `events` to add per-sponsor name/website/bio/on_ticket columns for both the top and bottom sponsor slots. Apply after `026_add_ticket_design_to_events.sql`. | `2024_01_01_003100_add_sponsor_details_to_events` |
| `033_create_support_requests.sql` | ☐ | CREATE `support_requests` table (in-dashboard Contact support tickets: `company_id` FK ON DELETE CASCADE, `user_id` FK ON DELETE SET NULL, `category` enum, `subject`, `message`, `status` enum default `open`, `access_consent` bool + `access_consent_at` recording the per-ticket "allow CK Enterprises to access my account" authorisation, `resolved_at`, timestamps, `(company_id, status)` index). Company-owned (tenant-scoped). Apply after `002_create_companies.sql` and `005_create_users.sql`. | `2024_01_01_003200_create_support_requests_table` |
| `034_create_event_sponsors.sql` | ☐ | CREATE `event_sponsors` table and backfill from the per-Event sponsor columns. Apply after `032_add_sponsor_details_to_events.sql`. | `2024_01_01_003300_create_event_sponsors_table` |
| `035_add_mail_transport_to_platform_settings.sql` | ☐ | ALTER `platform_settings` to add `mail_transport` enum(`smtp`,`graph`) NOT NULL DEFAULT `smtp` — the Platform-wide outbound-mail transport selector flipped by a Super_Admin on the Settings page. `smtp` (default) uses Laravel's configured cPanel SMTP mailer; `graph` uses the Microsoft Graph API `sendMail` transport sending from the organisation's own domain mailbox. `graph` only takes effect once the Graph credentials exist in the environment, otherwise the app falls back to SMTP, so the toggle is always safe to leave set. Credentials live in env/config, never the DB. Apply after `034_create_event_sponsors.sql`; requires `platform_settings` (003). | `2024_01_01_003400_add_mail_transport_to_platform_settings_table` |
| `036_add_description_to_ticket_types.sql` | ☐ | ALTER `ticket_types` to add a nullable `description` TEXT column (optional per-ticket-type blurb captured in the redesigned authoring accordion) placed after `name`. Existing rows adopt NULL, so behaviour is unchanged until an organiser enters a description. Apply after `035_add_mail_transport_to_platform_settings.sql`; requires `ticket_types` (008). | `2025_01_01_000000_add_description_to_ticket_types` |
| `037_allow_unlimited_capacity_mode.sql` | ☐ | DOCUMENTATION ONLY — records that `ticket_types.capacity_mode` now also accepts `unlimited` alongside `capped`/`shared_pool`. No schema change is needed: the column is already `varchar(20)` (020), not an ENUM, and the permitted value set is enforced in application code via `Rule::in(TicketType::MODES)`. Included to keep the numbered trail complete; contains only a defensive no-op re-asserting the existing column width and no `migrations` ledger row (no migration backs it). Apply after `036_add_description_to_ticket_types.sql`. | Application layer (`App\Models\TicketType::MODES`); no migration DDL |
| `038_add_cancelled_at_to_events.sql` | ☐ | ALTER `events` to add a nullable `cancelled_at` TIMESTAMP column (NULL = active) placed after `is_published`. Backs the deletion-vs-cancellation rule: an Event with no confirmed bookings can be deleted outright, while an Event that has taken bookings can only be cancelled — `cancelled_at` is stamped and the Event is unpublished, retaining every Order so customers can be contacted and refunds arranged via support. Existing rows adopt NULL, so behaviour is unchanged until an organiser cancels. Apply after `037_allow_unlimited_capacity_mode.sql`; requires `events` (006). | `2025_01_02_000000_add_cancelled_at_to_events` |
| `039_create_event_questions.sql` | ☐ | `event_questions` table (Company-owned per-Event custom purchase questions: `company_id` + `event_id` FKs cascading on delete, `type` (`free_text`/`select`/`number`), `label`, `options` JSON (select choices), `required` boolean, `position`). Up-to-three-per-event ceiling enforced in application code. Apply after `038_add_cancelled_at_to_events.sql`; requires `companies` (002) and `events` (006). | `2025_01_03_000000_create_event_questions_table` |
| `040_create_order_question_answers.sql` | ☐ | `order_question_answers` table (Company-owned per-Order custom question answers: `company_id` + `order_id` FKs cascading on delete, `event_question_id` FK nulled on delete, `question_label` snapshot, `answer` text, `captured_at` timestamp). Apply after `039_create_event_questions.sql`; requires `companies` (002), `orders` (009) and `event_questions` (039). | `2025_01_04_000000_create_order_question_answers_table` |
| `041_add_abuse_to_support_requests_category.sql` | ☐ | ALTER `support_requests` to add `abuse` to the `category` ENUM (NOT NULL, DEFAULT `other` unchanged), backing the public "Report this event" control at the bottom of every Event page — a visitor's abuse/misuse report is filed as a support ticket with `category = 'abuse'` so it reaches the Super_Admin support queue as a distinct bucket. Apply after `040_create_order_question_answers.sql`; requires `support_requests` (033). | `2025_01_05_000000_add_abuse_to_support_requests_category` |
| `042_create_error_reports.sql` | ☐ | CREATE `error_reports` table (production 500 capture: `reference` UNIQUE short customer-facing lookup code, nullable `company_id`/`user_id` FKs to companies/users ON DELETE SET NULL, `exception_class`, `message`, `file`/`line`, `status_code` default 500, `method`/`url`, `ip_address`, `user_agent`, `trace` longtext, JSON `context`, `resolved_at`, timestamps, `created_at` index). NOT tenant-scoped by the global scope — the admin surface reads across every tenant. Apply after `041_add_abuse_to_support_requests_category.sql`; requires `companies` (002) and `users` (005). | `2025_01_06_000000_create_error_reports_table` |
| `043_add_mfa_to_users.sql` | ☐ | ALTER `users` to add optional per-user TOTP two-factor authentication (RFC 6238): `two_factor_secret` (TEXT, ENCRYPTED at rest, NULL until enrolment), `two_factor_recovery_codes` (TEXT, ENCRYPTED JSON array of one-time codes, NULL until enrolment), `two_factor_confirmed_at` (TIMESTAMP, non-NULL only once the first code is verified and MFA is active — only such users are challenged at login) and `mfa_prompt_dismissed_at` (TIMESTAMP, stamped when the user picks "don't remind me again" on the post-login MFA recommendation nudge; NULL keeps nudging each login). MFA is opt-in and managed on the profile page. Apply after `042_create_error_reports.sql`; requires `users` (005). | `2025_01_08_000000_add_mfa_to_users_table` |
